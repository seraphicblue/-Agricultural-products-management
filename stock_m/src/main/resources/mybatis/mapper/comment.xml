<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="stock_m.dao.CommentDao">

    <insert id="commentSubmit" parameterType="stock_m.dto.CommentDto">

        INSERT INTO qnacomment (qnaboardid,  userid, content, regdate)
        VALUES (#{qnaboard.qnaboardid}, #{user.userid}, #{content}, now())
    </insert>
</mapper>
